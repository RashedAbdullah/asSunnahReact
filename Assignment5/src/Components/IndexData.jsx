import React from 'react'

const index = ({allData}) => {
  const [inputedData, setInputedData, setInputValue, setIsUpdate, setIndex] = allData;
  console.log(inputedData);
  return (
    <div>index</div>
  )
}

export default index;